services:
  redis:
    image: redis:6.2.14
    container_name: redis
    network_mode: host
    volumes:
      - /home/ubuntu/redis/data:/data
    logging:
      driver: fluentd
      options:
        fluentd-address: localhost:24224
        tag: db/{{.Name}}
    restart: always